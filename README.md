# asset-lint-example
Example repository that demonstrates asset-lint capabilities in CI environment.

Look at the example workflow. On the main branch it is passing,
but there is one open pull request where `asset-lint` found some errors.

